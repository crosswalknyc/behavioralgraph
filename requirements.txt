flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.6
python-socketio==5.11.0
python-engineio==4.8.1
eventlet==0.35.1
pandas==2.1.4
numpy>=1.26.0
setuptools>=70.0.0
clickhouse-connect
gunicorn==21.2.0
python-dotenv==1.0.0
boto3>=1.35.50  # 1.35.50+ adds IfNoneMatch on PutObject for atomic claims
certifi>=2024.1.0
pyopenssl>=24.0.0
# 2026-08-20: anthropic 1.0.0 rewrote its HTTP stack onto httpx2 and
# stopped pulling in classic httpx, which claude_client.py imports
# directly - every Claude call on Render died with "No module named
# 'httpx'". Pin to the proven 0.1xx line (0.102.0 runs everywhere
# else) and declare httpx explicitly. Same guard for openai: 2.x is
# the proven line on both prod boxes.
openai>=1.12.0,<3.0
anthropic>=0.100.0,<1.0
httpx>=0.27,<1.0
reportlab==4.0.4
weasyprint==60.1
google-auth==2.27.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.118.0
PyGithub==2.1.1
yt-dlp>=2025.10.14
requests>=2.32.0
# Trends IQ scrapers (AMC, Reddit, Instagram, Disney+, Best Buy, ReelShort,
# donated-cookie loader) use curl_cffi to impersonate real Chrome / Safari
# TLS handshake fingerprints. Sites like AMC (Akamai) and Reddit that only
# check JA3/JA4 wave curl_cffi requests through where stdlib requests +
# Playwright headless get bot-blocked.
curl_cffi>=0.13.0
# Trends IQ: richer Google Trends data than the public RSS.
# Wraps the same /_/TrendsUi/data/batchexecute endpoint the new
# trends.google.com/trending UI uses. Gives volume, growth %,
# started time, trend breakdown keywords, and per-trend news
# articles - a 30x depth increase over RSS (~370 US trends vs ~12).
trendspy>=0.1.6
# BPIQ "Deck with Analysis" exporter - migration/bpiq_deck_builder.py
# uses python-pptx for slide construction and Pillow for the
# Crosswalk wordmark + cover-image center-crop.
python-pptx>=1.0.0
Pillow>=10.4.0